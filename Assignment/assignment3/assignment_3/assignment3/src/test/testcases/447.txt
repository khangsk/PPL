
            var x, y, z: real;
                t, u: integer;
                k: boolean;
            procedure main();
            var u:boolean;
            begin
                u := u and k or u or k;
                z := t div 2 mod t;
                x := ((x + y * z) / t) + (t div t);
                t := ((t + 2 * t) / 3) + (t div 100);
                y := t / 0;
                k := (x > y) or else (y < z);
            end
        