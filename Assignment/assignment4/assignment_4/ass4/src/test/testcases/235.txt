

var a: array[0 .. 8] of integer;
procedure main();
var
    i, l, r: integer;
begin
    l := 0;
    r := 8;
    for i := l to r do begin
        a[i] := i + 1;
        putInt(a[i]); putString(" ");
    end
    putLn();
    for i := l to r do begin
        putInt(i); putString(": "); putIntLn(a[i]);
    end
end

