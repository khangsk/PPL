

procedure main();
var a: array[-8 .. 5] of integer;
    i, l, r: integer;
begin
    l := -8;
    r := 5;
    for i := l to r do begin
        a[i] := i + 1;
        putInt(a[i]); putString(" ");
    end
    putLn();
    for i := l to r do begin
        putInt(i); putString(": "); putIntLn(a[i]);
    end
end

