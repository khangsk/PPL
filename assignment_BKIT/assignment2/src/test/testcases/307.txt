
        Function: fact
        Body:
            If a[i] > b Then 
                writeln(a[i], " is larger than ", b);
            Else 
                writeln(a[i], " is smaller than ", b);
            EndIf.
        EndBody.
        